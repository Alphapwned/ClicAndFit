{{ form_start(form) }}
    {{ form_widget(form) }}
    <div class="d-flex">
        <div class="w-50">
            <a class="btn btn-lg btn-primary" href="{{ path('app_main') }}">Retour à l'accueil</a>
        </div>
        <div class="d-flex justify-content-end w-50 mb-3">
            <button class="btn btn-lg btn-primary">{{ button_label|default('Valider l\'annonce') }}</button>
        </div>

    </div>
{{ form_end(form) }}
